{
    "name": "website",
    "private": "true",
    "scripts": {
        "dev": "cd website && npm run dev",
        "install": "cd website && npm i",
        "deploy": "./deploy.sh"
    },
    "devDependencies": {
        "concurrently": "^9.1.2",
        "express": "^5.1.0"
    }
}

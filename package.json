{
    "name": "website",
    "private": "true",
    "scripts": {
        "dev": "cd website && npm run dev",
        "install": "cd website && npm i",
        "deploy": "./deploy.sh"
    },
    "devDependencies": {
        "concurrently": "^9.2.1",
        "express": "^5.1.1"
    }
}
